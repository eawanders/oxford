% ----- Other packages and settings -----
\usepackage{tabularray}
\UseTblrLibrary{booktabs}
\usepackage{siunitx}
\usepackage{float}
\usepackage{caption} % Load caption package once
\captionsetup[figure]{position=above, skip=10pt} % Consolidated and using 10pt skip as it was last
\floatstyle{plaintop}
\restylefloat{figure}

% Set Roman page numbering for front matter
\usepackage{etoolbox}

% Define \mainmatter to switch to Arabic numbering later
\newcommand{\mainmatter}{%
  \clearpage%
  \pagenumbering{arabic}%
}

% Suppress page number on the first page
\usepackage{fancyhdr}
\fancypagestyle{plain}{%
  \fancyhf{} % clear all header and footer fields
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0pt}
}